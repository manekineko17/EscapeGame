{% extends 'dashboard.html.twig' %}

{% block title %}Gérer mes réservations{% endblock %}

{% block body %}

<div class="row">

    <div class="col"></div>
        <h1 class="text-center my-3">Gérer mes réservations</h1>

        {# <div class="col-8">
            <br>
            {{ form_start(userForm) }}
            {{ form_widget(userForm) }}
            <button type="submit" class="btn btn-success">
                Enregistrer
            </button>
            <br><br>
            {{ form_end(userForm) }}
        </div> #}
    </div>
</div>
{% endblock %}