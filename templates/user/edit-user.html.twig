{% extends 'dashboard.html.twig' %}

{% block title %}Modifier mon compte{% endblock %}

{% block body %}

<div class="row">
    <h1 class="text-center my-3">Modifier mon compte</h1>

    <div class="col-8 my-3" style="width: 40rem;">
        {{ form_start(userForm) }}
        {{ form_widget(userForm) }}
        <button type="submit" class="btn btn-outline-success my-3">Enregistrer</button>
        {{ form_end(userForm) }}
    </div>
</div>
{% endblock %}