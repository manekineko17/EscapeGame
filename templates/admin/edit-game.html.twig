{% extends 'dashboard.html.twig' %}

{% block title %} Ajouter un game {{ parent() }} {% endblock %}

{% block body %}
<div class="row">
			<div class="col-2"></div>
                <div class="col-8">
                    <br>
                    <h3>Ajouter un scénario de game</h3>
                    
                        {{ form_start(addGameForm) }}
                            {{ form_widget(addGameForm) }}
                            <button type="submit" class="btn btn-outline-success" href="{{ path('admin_add_game')}}">
                                Valider
                            </button>
                            <br>
                            <br>
                        {{ form_end(addGameForm) }}
                </div>
            </div>

{% endblock %}