{% extends 'dashboard.html.twig' %}

{% block title %} Edit User {{ parent() }} {% endblock %}

{% block body %}
<div class="row">
	<div class="col">
			<h1 class="my-3 text-center">Modifier un user</h1>

			<div style="width: 50rem;">

				{{ form_start(userForm) }}
				{{ form_widget(userForm) }}
				<button type="submit" class="btn btn-outline-success my-3" href="{{ path('admin_users')}}">Valider
				</button>
				<br>
				{{ form_end(userForm) }}
				
			</div>
	</div>	
</div>

{% endblock %}