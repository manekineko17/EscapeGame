{% extends 'dashboard.html.twig' %}

{% block title %} Gérer les séances {{ parent() }} {% endblock %}

{% block body %}
<div class="row">
			<div class="col-2"></div>
                <div class="col-8">
                    <br>
                    <h3>Ajouter ou modifier une séance</h3>
                    
                        {{ form_start(addSessionForm) }}
                            {{ form_widget(addSessionForm) }}
                            <button type="submit" class="btn btn-outline-success" href="{{ path('admin_session')}}">
                               Valider
                            </button>

                            <br>
                            <br>
                        {{ form_end(addSessionForm) }}
                </div>
            </div>

{% endblock %}